# During development: leave this as /api
# Vite's proxy will forward /api/* → http://localhost:5000/api/*
# For production builds, change to your real backend URL e.g. https://your-api.com/api
VITE_API_URL=/api
